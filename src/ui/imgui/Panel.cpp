#include "umgebung/ui/imgui/Panel.hpp"

namespace Umgebung {
    namespace ui {
        namespace imgui {
            // The base Panel class doesn't have complex logic in its cpp file.
            // The constructor is defined in the header, and the destructor is defaulted.
            // The render function is pure virtual.
        }
    }
}